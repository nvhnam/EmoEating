fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
python-dotenv>=1.0
numpy>=1.26
funasr>=1.1.0
modelscope>=1.14.0
# torch/torchvision are installed separately in server/Dockerfile from
# PyTorch's dedicated CPU wheel index (NOT via this file) — see the
# Dockerfile comment for why that has to be an isolated pip command.
